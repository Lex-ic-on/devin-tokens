# devin-tokens

Aggregate [Devin CLI](https://devin.ai) token usage from local transcript files.

Reads `~/.local/share/devin/cli/transcripts/*.json` (auto-saved by Devin CLI) and prints daily / per-session summaries. No special flags or export steps needed.

## Install

```sh
# With uv (recommended)
uv tool install git+https://github.com/Lex-ic-on/devin-tokens.git

# Or from a local clone
git clone https://github.com/Lex-ic-on/devin-tokens.git
cd devin-tokens
uv tool install -e .      # editable — picks up local changes

# Also works with pip / pipx
pip install git+https://github.com/Lex-ic-on/devin-tokens.git
```

Requires Python 3.10+. No external dependencies (stdlib only).

## Usage

```sh
devin-tokens                        # last 14 days, daily summary
devin-tokens --days 7               # last 7 days
devin-tokens --since 2026-06-01     # from a specific date
devin-tokens --until 2026-06-30     # up to a specific date
devin-tokens --all                  # all sessions
devin-tokens --list                 # one row per session
devin-tokens --session rough-neon   # single session detail
```

## Example output

```
Devin CLI token usage  (2026-06-12 – 2026-06-26)

  date          sessions  in(+cache)    output      cached
  ------------------------------------------------------------
  2026-06-20           4   6,439,931    47,047   6,071,640  flint-room, ...
  2026-06-22           1   6,539,335    54,130   6,140,769  chief-street
  2026-06-26           3   5,923,817    33,862   5,499,947  rough-neon, ...
  ------------------------------------------------------------
  TOTAL               11  38,192,153   235,147  36,113,738
```

## Token columns

| Column | Meaning |
|---|---|
| `in(+cache)` | Total prompt tokens sent to the model (includes cached) |
| `output` | Completion tokens generated by the model |
| `cached` | Tokens served from cache (reduced cost) |

## Date basis

Sessions are grouped and filtered by **start time** (`steps[0].timestamp`).

## Per-step breakdown

Devin CLI changelog `2026.5.26-0` mentions per-step token fields in ATIF exports. The code is ready to display them when they appear in transcript files. Use `--session <id>` and look for `*`-marked sessions.

## License

MIT
